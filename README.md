# Distributed Job Scheduler

A minimal C++20 project skeleton for a distributed job scheduler.

## Windows

Build and run with PowerShell:

```powershell
.\scripts\build.ps1
.\build\scheduler_app.exe
```
